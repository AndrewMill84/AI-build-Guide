# AI Build OS Rules

This project uses the AI Build OS workflow system. All agents working in this workspace must adhere to the following rules:

- **Always read `STATUS.md`** in the current project before doing any work to understand context.
- **Follow the rules** listed in [AGENTS.md](file:///c:/Users/andre/AI%20Build%20Process/ai-build-os/AGENTS.md).
- **Use the markdown templates** from the `templates/` directory for all artifacts.
- **Update `STATUS.md`** immediately after completing any stage or significant action.
- **Check `memory/decisions.md` and `memory/patterns.md`** before making any major design decisions.
